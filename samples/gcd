gcd(a,b){
    IF(b==0) 
        RETURN a;
    ELSE 
        IF(a==0)
            RETURN b;
         ELSE
            IF(a<b)
                RETURN gcd(b-a,a);
            ELSE 
                RETURN gcd(b,a-b);
}
PRINT(gcd(42,14));